Due to version control limits on the JSON files, you must convert the *.csv to *.json.

This can be done using the Python script and a command similar to:

python csv_to_json.py NAME_OF_CSV_FILE.CSV


where NAME_OF_CSV_FILE.CSV is your local csv file.

Note: the script requires Python 2 (NOT 3).
